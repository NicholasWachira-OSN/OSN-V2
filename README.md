# OSN-V2

Monorepo for:
- `backend/` — Laravel API and admin panel
- `frontend/` — Vite + Vue 3 web app
- `mobile_app/` — Flutter app

## Getting started
See `docs/README.md` for setup across all apps.

## Pushing code
This repository is set up to ignore IDE files and common build artifacts at the root. Each subproject also has its own `.gitignore`.
